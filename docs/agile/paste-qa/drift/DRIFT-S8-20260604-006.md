# DRIFT-S8-20260604-006

| 字段 | 值 |
|------|-----|
| matrixRowId | S8M-SUM-004 |
| fixtureId | s8-summary-border-glow-probe |
| blockType | summary (highlight) |
| variantId | highlight_border_glow |
| variantType | probe |
| validatorStatus | WARNING |
| pasteStatus | FAIL |
| 测试人 | 维多 |
| 测试日期 | 2026-06-04 |

## 现象

**没有显示卡片边框和背景色**（含 `box-shadow` / 卡片容器未体现）。

## 三份材料

| 材料 | 路径 / 说明 |
|------|-------------|
| Clipboard HTML | `tests/snapshots/wechat-paste-qa/s8-summary-border-glow-probe.html` |

## 失真类型

CSS 被删除 · 表现异常（Yellow `box-shadow`）

## 根因假设

公众号剥离 `box-shadow` 与外层卡片样式；probe 验证 Yellow 边界失败。

## contractAction

`box-shadow` 保持 Yellow；probe 不进入 preset；考虑 fallback 纯色边框。

## follow-up story

Matrix / Contract 记录；不晋升 variant

## 状态

OPEN

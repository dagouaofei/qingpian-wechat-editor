---
description: Execution Report 固定流程（主事实源）
alwaysApply: true
---

# Execution Report 治理

> 协作摘要见 `.cursor/rules/agile-rules.mdc` · 模板见 `docs/agile/execution-reports/_template.md`

## 固定顺序

1. 完成实际工作并运行检查；
2. 提交**主要成果 commit**（IMPLEMENTATION_COMMIT）；
3. 使用**真实 commit hash**和**真实检查结果**完成 execution report；
4. 单独提交 **report-only commit**（REPORT_COMMIT）；
5. report-only commit **不回填**报告正文；
6. 最终回复报告 **HEAD at review time**（= REPORT_COMMIT，仅出现在 Cursor 最终回复）。

## 报告填写要求

- 最终报告**不得**保留：`待执行` · `待 commit` · `待回填` · `TBD`；
- 未运行的检查必须写 **`未运行：具体原因`**；
- **主要成果 commit** 必须写入报告（真实 hash + message）；
- report-only commit **不要求**在报告中自引用其 hash；
- **不得**为回填最新 HEAD 而循环修改报告或反复 report-only commit。

## 状态语义

- **已 commit** ≠ **已 merge** ≠ **已 push**；
- Story **Done** 须 PO 验收依据；Sprint **Closed** 须 PO 关闭授权。

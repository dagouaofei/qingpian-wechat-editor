---
description: 样式系统长期技术约束（theme / preset / variant / registry）
globs: src/core/styles/**,docs/architecture/style-system.md
alwaysApply: false
---

# 样式系统规则

## 正式交付范围

- 样式系统是产品正式交付范围的长期核心能力，不是后置预留或临时页面样式
- 正式交付范围以当前 Approved Release / Sprint 文档及 `docs/agile/release-plan.md`、`docs/agile/releases/` 事实源为准
- 未做完整样式市场时，仍须按正式样式系统实现 registry、variant、assignment 等架构
- 实际 variant、block 数量与生命周期 tier 以 registry、数据库及当前代码事实为准；代码或枚举中的 `release1_required` 等标识是正式兼容字段，不表示当前 Release 编号或范围状态

## 架构方向

样式方案必须考虑：

- theme
- style preset
- style variant
- block style registry
- style assignment
- slot
- density
- layout
- spacing
- typography
- decoration

## 扩展目标

- 样式系统要支持未来「样式千变万化」的扩展目标
- 不得把样式写死为不可复用的页面样式

## 复制一致性

- 涉及样式的任务必须考虑公众号复制一致性
- Preview Renderer 和 Copy Renderer 必须共享同一套样式定义

## 旧项目经验

- 可参考旧一键成稿项目中的 Component DSL、slot、density、variant、registry 等探索经验
- 但不得照搬旧项目混杂方案（Visual Layer、Space Style 等）

## 治理边界

敏捷治理、验收闸门、分支、merge、push 与 Story 状态遵循 `.cursor/rules/agile-governance.mdc`。本文件仅定义样式系统领域技术约束。

## 参考文档

- `docs/architecture/style-system.md`
- `docs/architecture/wechat-copy-style-rules.md`

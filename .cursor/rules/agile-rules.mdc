---
description: 敏捷项目管理规则
alwaysApply: true
---

# 敏捷规则

## 任务管理

- 所有任务必须对应 **Story / Bug / Decision**
- 每个 Story 必须有验收标准（AC）
- 每个 Sprint 必须更新 `docs/agile/sprint-backlog.md`
- **每个 Story / Bug / Docs / Chore 任务应对应一个独立工作分支**
- `sprint-backlog.md` 中应记录任务分支名（如有）

## Git 分支与任务

- 每个 Release 从 `main` 新建 `release/<n>` 分支（Release 1：`release/1`）
- 每个 Sprint 从**当前 release 主干**新建 `sprint/<sprint-slug>` 分支
- Sprint 内任务从**当前 sprint 分支**切出 `feature/` / `docs/` / `bugfix/` / `chore/` 分支
- 一个工作分支只解决一个问题
- 工作分支完成后合并回 sprint 分支，**不直接合并 release 或 main**
- 详见 `docs/agile/git-workflow.md`

## 范围变更

- 涉及需求范围变化时，必须更新 Product Backlog 或 Release Scope
- 重大决策记录到 `docs/agile/decisions.md`

## Sprint 周期原则

- 后续 Sprint 可按合理工作量拆分
- 每个 Sprint 必须更新 `docs/agile/sprint-backlog.md`

## Execution Report（默认必须）

每轮 Cursor 执行完成后，**必须**生成一份 execution report。

### 存放路径

```text
docs/agile/execution-reports/
```

### 命名规则

```text
YYYY-MM-DD-<short-task-slug>.md
```

示例：

```text
2026-05-30-s1b-execution-report-workflow.md
2026-05-30-style-system-review.md
2026-05-31-article-schema-contract.md
```

- 文件名**必须以日期开头**

### 每份 report 至少包含

- 任务背景
- 分支名（当前分支）
- **来源分支**
- **目标合并分支**
- 关联 Sprint / Story / Bug / Decision
- 本轮目标
- 执行范围
- 修改文件
- 新增文件
- 未修改但阅读过的关键文件
- 关键决策
- 验收标准完成情况
- 运行的检查命令
- 检查结果
- 未完成事项
- 风险与阻塞
- 需要用户 / ChatGPT 继续审查的问题
- 建议下一步
- commit hash（若本轮无 commit，写明「未提交 / not committed」）

### 默认行为

- 项目规则已默认要求 execution report，**无需在每条 Cursor 指令中重复要求**
- 仅当用户**明确要求本轮不生成** execution report 时，才可跳过，并在 Cursor 回复中说明原因
- 使用 [`docs/agile/execution-reports/_template.md`](../../docs/agile/execution-reports/_template.md) 作为模板

### 固定顺序与提交

1. 完成实际工作并运行检查；
2. 提交**主要成果 commit**；
3. 使用**真实 commit hash**和**真实检查结果**完成 execution report；
4. 单独提交 **report-only commit**；
5. report-only commit **不回填**报告正文；
6. 最终回复报告 **HEAD at review time**（= report-only commit，仅出现在 Cursor 最终回复）。

### 最终报告要求

- 不得保留未完成占位状态（如检查或 commit 尚未填写）；
- 未运行的检查必须写 **`未运行：具体原因`**；
- 必须记录**主要成果 commit**；
- **影响实际成果的修正 commit** 也必须记录（如有）；
- report-only commit **不要求**在报告中自引用其 hash；
- **不得**为回填最新 HEAD 而循环修改报告或反复 report-only commit。
- **已 commit** ≠ **已 merge** ≠ **已 push**。

### Sprint / Story 关闭

- Cursor **不应仅凭自己的总结**宣布 Sprint 或 Story 完成
- **任务完成状态不等于 Sprint 关闭**；Sprint 关闭必须由**用户确认**
- Sprint / Story 的最终关闭需要**用户确认**
- 存在争议时，状态标记为 `In Review` 或 `Needs Review`，而非直接 `Done`

## 文档位置

- Product Backlog：`docs/agile/product-backlog.md`
- Sprint Plan：`docs/agile/sprint-plan.md`
- Sprint Backlog：`docs/agile/sprint-backlog.md`
- Bug Backlog：`docs/agile/bugs.md`
- 决策记录：`docs/agile/decisions.md`
- 变更日志：`docs/agile/changelog.md`
- Execution Reports：`docs/agile/execution-reports/`
